@model MEC.Domain.Entity.School.Announcement

@{
    ViewData["Title"] = "Duyuru Düzenle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="announcement-form-page">
    <div class="announcement-form-shell">
        <a asp-action="Index" class="announcement-form-back">Geri Don</a>

        <div class="announcement-form-header">
            <div class="announcement-form-header__copy">
                <span class="announcement-form-header__eyebrow">Duyuru yönetimi</span>
                <h1>Duyuru Düzenle</h1>
                <p>Başlık, içerik ve yayın durumunu güncelleyerek duyuruyu portal akışına uygun hale getirin.</p>
            </div>
        </div>

        @if (TempData["AnnouncementSuccess"] is string successMessage && !string.IsNullOrWhiteSpace(successMessage))
        {
            <div class="announcement-form-alert announcement-form-alert--success">@successMessage</div>
        }

        <div class="announcement-form-card">
            <form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="announcement-form">
                <input asp-for="Id" type="hidden" />

                <div asp-validation-summary="ModelOnly" class="announcement-form-alert announcement-form-alert--error"></div>

                <div class="announcement-form-grid">
                    <div class="announcement-form-field announcement-form-field--full">
                        <label asp-for="Title">Duyuru Başlığı</label>
                        <input asp-for="Title" class="announcement-form-input" placeholder="Duyuru başlığını giriniz" required />
                        <span asp-validation-for="Title" class="announcement-form-validation"></span>
                    </div>

                    <div class="announcement-form-field announcement-form-field--full">
                        <label asp-for="Content">Duyuru İçeriği</label>
                        <textarea asp-for="Content" id="summernote" class="announcement-form-input announcement-form-input--editor" required></textarea>
                        <span asp-validation-for="Content" class="announcement-form-validation"></span>
                    </div>

                    <div class="announcement-form-field announcement-form-field--full">
                        <div class="announcement-form-toggle">
                            <span class="announcement-form-toggle__label">Aktiflik durumu</span>

                            <div class="announcement-form-switch-wrap">
                                <span class="announcement-form-switch__state">Pasif</span>
                                <input asp-for="IsActive" id="announcementIsActive" class="announcement-form-switch__input" type="checkbox" />
                                <label for="announcementIsActive" class="announcement-form-switch__control" aria-label="Aktiflik durumunu degistir">
                                    <span class="announcement-form-switch__thumb"></span>
                                </label>
                                <span class="announcement-form-switch__state announcement-form-switch__state--active">Aktif</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="announcement-form-actions">
                    <button type="submit" class="announcement-form-actions__save">Kaydet</button>
                    <a asp-action="Index" class="announcement-form-actions__cancel">İptal</a>
                </div>
            </form>
        </div>
    </div>
</section>

@section Scripts {
    <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#summernote').summernote({
                placeholder: 'Duyuru detaylarini yazin, link veya gorsel ekleyin...',
                tabsize: 2,
                height: 320,
                toolbar: [
                    ['style', ['style']],
                    ['font', ['bold', 'underline', 'clear']],
                    ['color', ['color']],
                    ['para', ['ul', 'ol', 'paragraph']],
                    ['table', ['table']],
                    ['insert', ['link', 'picture', 'video']],
                    ['view', ['fullscreen', 'codeview']]
                ]
            });
        });
    </script>
}
