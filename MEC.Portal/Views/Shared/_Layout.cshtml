<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MEC Portal</title>
    <!-- Bootstrap ve CSS Tanımlamaları -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="bg-light">
    <header>
        <!-- Navbar (Üst Menü) Başlangıcı -->
        <nav class="navbar navbar-expand-lg navbar-white bg-white border-bottom shadow-sm mb-4 py-3">
            <div class="container-fluid px-4">

                <!-- EN SOL: Logo -->
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="~/content/images/logo.png" alt="MEC Logo" style="height: 40px;" />
                </a>

                <!-- Mobil ekranlar için açılır menü butonu -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- EN SAĞ: Profil ve Çıkış Butonları -->
                <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
                    <ul class="navbar-nav align-items-center">
                        <li class="nav-item me-3">
                            <span class="text-muted fw-bold">Hoş Geldiniz, @User.Identity?.Name</span>
                        </li>
                        <li class="nav-item me-2">
                            <a class="btn btn-outline-primary" asp-controller="Profile" asp-action="Index">
                                Profilim
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-danger" asp-controller="Account" asp-action="Logout">
                                Çıkış Yap
                            </a>
                        </li>
                    </ul>
                </div>

            </div>
        </nav>
    </header>

    <div class="container-fluid px-4">
        <main role="main" class="pb-3">
            <!-- Diğer sayfaların (Index vb.) yükleneceği alan -->
            @RenderBody()
        </main>
    </div>

    <!-- JS Tanımlamaları -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>